# AGENTS.md

## 프로젝트 목표

이 저장소의 목표는 **Paynesto**라는 모바일 앱을 만드는 것입니다.

이 프로젝트는 현실적인 구독 관리 + 월급 기반 돈 관리 제품처럼 보여야 하며, 다음 네 가지 핵심 영역으로 구성됩니다.

- **Home**
  - 월간 총 구독비
  - 다음 결제 예정
  - 월급 대비 구독비 상태
  - 절약 추천 카드
  - USD 구독의 KRW 예상 결제 요약

- **Subscriptions**
  - 구독 목록
  - 구독 등록 / 수정 / 삭제
  - 카테고리 / 통화 / 결제주기 필터
  - 무료체험 종료 관리
  - USD 결제 예상 금액 표시

- **Money Plan**
  - 세후 월급 입력
  - 고정비 입력
  - 월급 대비 고정비 / 구독비 분석
  - 추천 예산안
  - 절약 / 해지 가이드

- **My Page**
  - 프로필
  - 알림 설정
  - 프리미엄 결제
  - 이용약관 / 개인정보처리방침
  - 문의 및 지원

핵심 목표는 사용자가 자신의 구독 지출을 쉽게 파악하고, 월급에 맞는 현실적인 돈 관리 가이드를 받을 수 있는, 유지보수 가능한 React Native 앱을 만드는 것입니다.

## 제품 원칙

- 화려한 데모보다 **실제 생활에서 쓸 수 있는 흐름**을 우선합니다.
- 숫자만 보여주기보다 **행동 가능한 추천**을 더 중요하게 봅니다.
- 화면마다 일회성으로 구현하기보다, **UI와 도메인 모듈을 재사용 가능하게 설계**합니다.
- 결제일, 상태 배지, 추천 이유, 알림, 환율 반영 등 **실제 서비스처럼 믿을 수 있는 흐름**을 만듭니다.
- 별도 요청이 없다면, 이 제품과 무관한 도메인이나 과한 기능은 추가하지 않습니다.
- 초기 버전은 복잡한 금융 자문 서비스가 아니라, **구독 관리 및 예산 추천 도구**로 유지합니다.

## 기본 기술 방향

- 프로젝트를 처음부터 구성한다면 **React Native + TypeScript**를 사용합니다.
- 앱 구조는 **feature-based architecture**를 우선합니다.
- 내비게이션은 `React Navigation` 또는 `Expo Router` 중 하나를 일관되게 사용합니다.
- 전역/로컬 상태는 필요 범위에 맞게 최소화하며, 가능하면 **TanStack Query + Zustand** 조합을 우선 검토합니다.
- 패키지 매니저가 정해져 있지 않다면 **npm**을 우선합니다.
- PowerShell 환경에서 스크립트 실행 이슈가 있으면 `npm` 대신 `npm.cmd`를 사용합니다.
- 이미 lockfile이 있다면 기존 패키지 매니저를 따릅니다.
- 스타일링 시스템은 일관되게 유지합니다.
- 특별한 이유 없이 여러 UI 시스템을 섞지 않습니다.
- 초기에는 실제 은행/카드 연동보다, **명확한 API 경계와 수동 입력 기반의 v1**을 우선합니다.

## 핵심 도메인

제품은 아래 엔티티를 중심으로 모델링합니다.

- user
- user financial profile
- subscription
- notification settings
- budget report
- premium transaction
- exchange rate snapshot
- subscription fx estimate

## 필수 화면 및 기능

### 사용자 영역

- 온보딩
- 로그인 / 회원가입
- 홈 대시보드
- 구독 목록 / 상세 / 생성 / 수정
- 월급 / 고정비 입력 화면
- 예산 추천 결과 화면
- 알림 설정 화면
- 프리미엄 결제 화면

### 핵심 기능

- 구독 등록 / 수정 / 삭제
- 월 / 연 결제 금액 환산
- 결제 예정일 관리
- 무료체험 종료 관리
- 월급 기반 예산 추천
- 해지 후보 추천
- USD 구독의 KRW 예상 결제 금액 계산
- 환율 변동 감지
- 프리미엄 기능 게이트

## 권장 디렉터리 구조

저장소에 더 적절한 구조가 이미 없다면, 아래 구조를 기본으로 사용합니다.

- `src/app/`: 앱 엔트리, providers, navigation
- `src/features/`: 도메인 중심 기능 모듈
- `src/components/ui/`: 재사용 가능한 UI 기본 컴포넌트
- `src/components/shared/`: 공통 조합 컴포넌트
- `src/lib/`: 유틸리티, 포맷터, 상수, 헬퍼
- `src/services/`: 외부 서비스, Firebase, 알림, analytics, fx API 연동
- `src/types/`: 공통 타입
- `src/mocks/` 또는 `src/fixtures/`: 데모 / 시드 데이터
- `tests/`: 단위 테스트 / 통합 테스트
- `docs/`: 아키텍처 문서, 계획서, 트레이드오프 정리

## 엔지니어링 규칙

- **TypeScript strict mode**를 사용합니다.
- 특별한 이유가 없다면 `any`는 사용하지 않습니다.
- 컴포넌트는 작고, 역할이 명확하며, 조합 가능하게 유지합니다.
- 기능별 훅, 테스트, 타입은 가능하면 해당 기능 가까이에 둡니다.
- 비즈니스 로직은 화면 컴포넌트 안에 직접 넣지 말고 `hooks`, `services`, `utils`, `domain` 레이어로 분리합니다.
- 재사용성을 높일 수 있다면 설정 기반 리스트/폼 구성을 우선합니다.
- 모든 비동기 화면은 반드시 아래 상태를 포함해야 합니다.
  - 로딩
  - 빈 상태
  - 에러
  - 성공
- 위험도가 높은 액션에는 반드시 아래가 포함되어야 합니다.
  - 확인 절차
  - 성공/실패 피드백
- 상태 문자열은 여기저기 하드코딩하지 말고 enum 또는 상수로 관리합니다.
- 에러를 조용히 무시하지 않습니다.
- 죽은 코드, 주석 처리된 코드, 의미 없는 TODO는 남기지 않습니다.

## 환율 기능 규칙

- 보호가 필요한 외부 환율 API 키는 클라이언트 앱에 직접 넣지 않습니다.
- 환율 데이터는 반드시 **서버 함수 / 프록시 / 백엔드 레이어**를 통해 가져옵니다.
- USD 구독에는 항상 아래 값을 표시합니다.
  - 원래 금액(USD)
  - 현재 KRW 예상 금액
  - 적용 환율
  - 갱신 시각
- 카드사 수수료, 환전 스프레드, 정산 시점 차이로 실제 결제액이 달라질 수 있으므로, 필요한 경우 **예상 범위**를 제공합니다.
- 환율 데이터는 캐시를 사용하고, 새 데이터를 못 가져오면 마지막 유효 환율로 fallback 할 수 있어야 합니다.
- 이전 예상 금액과 현재 예상 금액 차이가 임계치를 넘으면 변동 상태를 계산할 수 있어야 합니다.
- 프리미엄 사용자에게만 환율 급변 알림 기능을 열어도 됩니다.

## UX 및 접근성 기준

- 모바일 화면은 특별한 이유가 없다면 다음 기준을 지원해야 합니다.
  - 명확한 헤더
  - 입력 검증
  - 로딩 상태
  - 빈 상태
  - 에러 상태
- 폼은 가능한 짧고 이해하기 쉽게 유지합니다.
- 금액은 통화 포맷을 일관되게 사용합니다.
- 날짜는 사용자 기준으로 이해하기 쉽게 표시합니다.
- 버튼/아이콘 버튼은 접근 가능한 이름이 있어야 합니다.
- 바텀시트, 모달, 드로어를 사용할 경우 닫기 흐름과 포커스/탭 이동을 고려합니다.
- 차트는 장식용이 아니라, 실제로 돈 관리에 의미 있는 정보를 제공해야 합니다.

## 성능 기준

- 클라이언트 상태는 가능한 한 작게 유지합니다.
- 불필요한 재렌더링과 과도한 번들 크기를 피합니다.
- 메모이제이션은 실제로 필요할 때만 사용합니다.
- 홈 대시보드는 빠르게 열려야 하며, 빈 화면보다 스켈레톤/로딩 UI를 우선합니다.
- 환율 API 호출은 과도하게 반복하지 않도록 캐시 및 stale 정책을 둡니다.

## 테스트 및 검증

단순하지 않은 변경에는 테스트를 추가하거나 기존 테스트를 갱신합니다.

최소한 아래 항목은 핵심 테스트 범위에 포함되어야 합니다.

- 구독 생성 / 수정 / 삭제
- 월간 구독비 합산
- 구독비 대비 월급 비율 계산
- USD -> KRW 예상 결제액 계산
- 환율 변동 상태 계산
- 프리미엄 기능 가드
- 알림 예약 로직

작업을 마무리하기 전에, 가능한 경우 아래 검증을 실행합니다.

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run test`

앱 빌드 환경이 준비되어 있다면 아래도 검토합니다.

- `npm.cmd run android`
- `npm.cmd run ios`

해당 명령이 없다면 적절히 추가하거나, 실행하지 못한 이유를 명확히 남깁니다.

## 계획 및 작업 방식

- 여러 화면, 여러 기능, 또는 아키텍처 결정이 포함된 작업은 먼저 `docs/execplans/<task-name>.md`를 작성합니다.
- 실행 계획 문서에는 아래 내용을 포함합니다.
  - 목표
  - 가정
  - 범위
  - 구현 단계
  - 리스크
  - 검증 방법
- 한 번에 크게 갈아엎는 방식보다, 작고 리뷰하기 쉬운 변경을 선호합니다.
- 사소한 모호함 때문에 멈추지 말고, 근거 있는 가정을 세운 뒤 문서에 남깁니다.

### 단계별(step) 작업 규칙

- 이 프로젝트는 `step/NN-<slug>` 브랜치 단위로 작업합니다.
- 새 step을 시작하기 전에 항상 직전 step 브랜치를 먼저 `develop`에 머지하고 원격 `develop`까지 푸시합니다.
- 새 step은 항상 최신 `develop`에서 새 브랜치를 따서 시작합니다.
- 각 step은 구현 전에 아래 두 문서를 반드시 먼저 만듭니다.
  - `docs/execplans/step-NN-<slug>.md`
  - `docs/steps/bootstrap-step-NN-<slug>.md`
- `docs/execplans` 문서와 `docs/steps` 문서는 같은 step 번호와 slug를 사용해 짝을 맞춥니다.
- step 구현이 끝나면 아래 순서대로 마무리합니다.
  - `npm.cmd run lint`
  - `npm.cmd run typecheck`
  - `npm.cmd run test`
  - Conventional Commit 메시지로 커밋
  - step 브랜치를 원격에 푸시
- 다음 step으로 바로 넘어갈 때도 위 흐름을 반복합니다.
  - 현재 step 브랜치를 `develop`에 머지
  - 원격 `develop` 푸시
  - 새 `step/NN-<slug>` 브랜치 생성
  - 계획서 2종 작성 후 구현 시작

## 완료 기준

아래 조건을 모두 만족해야 작업이 완료된 것으로 봅니다.

- 요청한 동작이 실제로 작동한다.
- 변경된 화면에 빈 상태 / 에러 / 로딩 상태가 포함되어 있다.
- 타입 안정성이 유지된다.
- 관련 테스트와 검증이 갱신되고 실행되었다.
- 동작이나 아키텍처가 크게 바뀌었다면 README 또는 문서가 함께 업데이트되었다.

## 유지보수 규칙

같은 실수나 동일한 리뷰 코멘트가 두 번 이상 반복되면, 이후 작업에도 반영될 수 있도록 이 파일에 해당 내용을 추가합니다.

## Git 커밋 메시지 규칙

- 커밋 메시지는 기본적으로 **Conventional Commits** 형식을 사용합니다.
- 기본 형식은 `type(scope): summary` 입니다.
- `summary`는 명령형 현재 시제로 짧고 명확하게 작성합니다.
- 가능하면 50~72자 안쪽으로 유지하고, 무엇을 바꿨는지 중심으로 씁니다.
- 이 프로젝트에서 우선 사용하는 `type`은 다음과 같습니다.
  - `feat`: 기능 추가
  - `fix`: 버그 수정
  - `refactor`: 동작 변화 없는 구조 개선
  - `docs`: 문서 변경
  - `test`: 테스트 추가 또는 수정
  - `chore`: 설정, 의존성, 보일러플레이트 정리 등 기타 작업
  - `style`: 포맷팅 등 동작과 무관한 스타일 변경
  - `perf`: 성능 개선
- 예시
  - `feat(subscriptions): add usd estimate card`
  - `fix(money-plan): correct salary ratio calculation`
  - `chore(repo): bootstrap react native app`
  - `docs(planning): add step 01 execution plan`

## 디자인 단위 규칙

- 디자인과 스타일 구현에서 크기 단위는 기본적으로 `px`만 사용합니다.
- `rem`, `em` 등 상대 단위는 특별한 예외 요청이 없는 한 사용하지 않습니다.
- spacing, radius, width, height, inset 등 수치형 스타일은 `px` 기준으로 명시합니다.
- React Native 스타일에서 숫자는 `px` 의도로 취급합니다.
